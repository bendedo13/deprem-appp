# Nginx — Deprem App reverse proxy
# Mevcut sitenizle çakışmaması için bu dosyayı ayrı bir server block olarak ekleyin.
#
# Kullanım:
# 1) Subdomain ile: deprem.siteniz.com -> bu block
# 2) Farklı port ile: 46.4.123.77:8001 üzerinden erişim (upstream port 8000)
#
# Bu dosyayı /etc/nginx/sites-available/depremapp kopyalayıp
# ln -s /etc/nginx/sites-available/depremapp /etc/nginx/sites-enabled/
# systemctl reload nginx

# Örnek: Subdomain ile (SSL varsa)
# server {
#     listen 80;
#     server_name deprem.siteniz.com;
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300;
#         proxy_connect_timeout 300;
#         proxy_send_timeout 300;
#     }
#     location /ws/ {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#     }
# }

# Örnek: Sadece port 8001 (mevcut site 80/443'te kalır)
# Bu block'u mevcut nginx config'inize ekleyin veya ayrı include edin
upstream deprem_backend {
    server 127.0.0.1:8000;
}
server {
    listen 8001;
    server_name _;
    location / {
        proxy_pass http://deprem_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    location /ws/ {
        proxy_pass http://deprem_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
